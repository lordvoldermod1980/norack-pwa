# SPA fallback: serve index.html for client-side routes (e.g. /tablet) on hard refresh.
# /api/* is handled by Pages Functions (auto-excluded via generated _routes.json),
# and real static assets are served before this rule applies.
/*    /index.html   200
